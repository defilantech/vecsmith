FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/ src/

EXPOSE 8080

ENV VECSMITH_HOST=0.0.0.0
ENV VECSMITH_PORT=8080

CMD ["python", "-m", "src.orchestrator.app"]
