apiVersion: v1
kind: Service
metadata:
  name: vecsmith-orchestrator
  namespace: vecsmith
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vecsmith-orchestrator
  ports:
    - port: 8080
      targetPort: 8080
      name: http
